import styles from './headerButton.module.scss'
const HeaderButton = () => {
  return (
    <div className={styles.headerBtn}>
        <button className={styles.btn}>get access</button>
    </div>
  )
}

export default HeaderButton
